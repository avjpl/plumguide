import React from 'react';

import SVGIcon from '../../ui/SVGIcon';

const SearchItem = () => {
  return (
    <li className='menu__item menu__item--left_border'>
      <SVGIcon icon={'search'} />
    </li>
  )
}

export default SearchItem;
